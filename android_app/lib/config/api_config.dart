class ApiConfig {
  // Change this to your Railway backend URL in production
  static const String apiBaseUrl = 'http://10.0.2.2:3000'; // Android emulator → localhost
  static const Duration timeout = Duration(seconds: 30);
}
